/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package firstgradledemo.app;

import firstgradledemo.list.LinkedList;

import static firstgradledemo.utilities.StringUtils.join;
import static firstgradledemo.utilities.StringUtils.split;
import static firstgradledemo.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
